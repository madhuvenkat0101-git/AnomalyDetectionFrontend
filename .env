# Copy to .env
# Base URL of the Node anomaly service (Socket.IO + REST live here).
VITE_API_URL=http://localhost:4000
# API key for the secured /api/alerts endpoint (must match the service's API_KEY).
VITE_API_KEY=change-me-to-a-long-random-secret